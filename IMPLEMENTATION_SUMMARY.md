# 会战排名定期记录功能 - 修改总结

## 项目概述
在项目中成功集成了**会战排名定期记录功能**，可以每30分钟自动获取会战排名前60名的数据并记录。

## 核心功能

### ✅ 自动排名获取
- 每30分钟自动触发一次排名数据获取
- 调用API 6次获取前60名排名（每页10名）
- 自动过滤和存储排名数据

### ✅ 数据持久化
- 使用SQLite数据库存储排名数据
- 自动清理30天前的数据
- 支持按时间戳查询排名历史

### ✅ 用户交互
- 【查看排名】 - 快速查看排名前10和本公会排名
- 【排名详情】 - 查看完整的前60名排名列表

## 文件修改清单

### 1. clanbattle/sql.py
**新增内容：**
- `RankingDao` 类 - 用于排名数据的数据库操作
  - `add_ranking()` - 添加排名记录
  - `get_latest_ranking()` - 获取最新排名
  - `get_ranking_by_time()` - 按时间查询
  - `get_all_timestamps()` - 获取所有时间戳
  - `refresh()` - 清理旧数据

### 2. clanbattle/model.py
**修改内容：**
- 导入：添加 `time`, `asyncio`, `RankingDao`
- `init_database()` - 初始化 `RankingDao` 和 `last_ranking_fetch` 
- 新增方法：
  - `fetch_period_ranking(page)` - 获取单页排名
  - `record_period_ranking()` - 定期执行排名记录（30分钟间隔）
  - `save_ranking(ranking_data)` - 保存排名到数据库
- 在监控循环中调用排名记录方法

### 3. clanbattle/__init__.py
**修改内容：**
- 导入：添加 `RankingDao`
- 在监控循环中添加 `await clan_info.record_period_ranking()` 调用
- 新增命令处理函数：
  - `view_ranking()` - 处理【查看排名】和【排名记录】命令
  - `view_ranking_detail()` - 处理【排名详情】命令
- 定时任务 `init_cb()` - 添加 `RankingDao.refresh()` 调用
- 更新帮助文本，添加新命令说明

## 实现细节

### API调用参数
```json
{
  "clan_id": "长老所在公会ID",
  "clan_battle_id": "当前会战ID",
  "period": 1,
  "month": 0,
  "page": "0-5（共6页，每页10名）",
  "is_my_clan": 0,
  "is_first": 1
}
```

### 数据库表结构
```sql
CREATE TABLE period_ranking (
    rank INT NOT NULL,          -- 排名位置
    clan_id INT NOT NULL,       -- 公会ID
    clan_name VARCHAR(32),      -- 公会名称
    leader_id INT NOT NULL,     -- 会长ID
    leader_name VARCHAR(16),    -- 会长名称
    damage INT NOT NULL,        -- 总伤害
    timestamp INT NOT NULL      -- 记录时间
)
```

### 时间控制机制
- **记录间隔**：30分钟（1800秒）
- **API调用间隔**：0.5秒（避免请求过快）
- **数据保留期**：30天
- **定时清理**：每天凌晨4:59

## 用户界面

### 命令1：查看排名
```
输入：【查看排名】 或 【排名记录】
输出：
==========当前排名前10==========
1. 公会A - 1,000,000伤害
2. 公会B - 900,000伤害
...
本群排名：
15. 本群公会 - 500,000伤害
```

### 命令2：排名详情
```
输入：【排名详情】
输出：
==========排名列表（前60）==========
1. 公会A (会长A) - 1,000,000伤害
2. 公会B (会长B) - 900,000伤害
3. 公会C (会长C) - 850,000伤害
...
60. 公会Z (会长Z) - 100,000伤害
```

## 工作流程

```
启动监控 【出刀监控】
    ↓
进入监控循环（每秒检查一次）
    ↓
每30分钟触发一次 record_period_ranking()
    ↓
调用 fetch_period_ranking() 6次（page 0-5）
    ↓
提取所有排名数据
    ↓
保存到 RankingDao
    ↓
用户可查询【查看排名】或【排名详情】
```

## 错误处理

- 单页获取失败继续获取下一页，不中断全流程
- API调用异常记录到日志，不影响监控继续
- 数据保存失败时输出错误信息到日志

## 性能考虑

- 排名记录采用30分钟间隔，不会频繁调用API
- 每次获取6页排名，总共需要6个API请求 + 间隔
- 数据库查询采用时间戳索引，查询性能优良
- 自动清理机制保持数据库大小在可控范围

## 兼容性

- ✅ 与现有出刀监控功能完全兼容
- ✅ 与现有所有数据库操作兼容
- ✅ 与现有定时任务体系兼容
- ✅ 不修改现有API调用逻辑

## 测试建议

1. 启动出刀监控，观察日志
2. 等待30分钟，验证排名记录触发
3. 发送【查看排名】命令，验证输出
4. 发送【排名详情】命令，验证完整列表
5. 观察凌晨4:59定时任务执行情况

## 常见问题

**Q: 为什么看不到排名数据？**
A: 确保出刀监控已启动，且已等待至少30分钟以获得第一条记录。

**Q: 排名如何更新？**
A: 自动每30分钟更新一次，只要监控在运行中。

**Q: 数据会保留多久？**
A: 保留30天，每天凌晨4:59自动清理超期数据。

**Q: 能否手动触发排名记录？**
A: 目前不支持，但可以在监控循环中修改时间间隔。

## 后续可能的扩展

1. 添加排名变化趋势分析
2. 支持手动触发排名记录
3. 添加排名段位分类统计
4. 支持导出排名数据为CSV
5. 添加排名对比功能（与前一次对比）
